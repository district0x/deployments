version: "3"
services:

  ganache:
    image: trufflesuite/ganache-cli:6.1.7
    container_name: dev_ganache
    command:
      --deterministic
      --mnemonic district0x
      --port 8545
      --gasLimit 8000000
      --db /ganache-db
      --noVMErrorsOnRPCResponse
    volumes:
      - /home/$USER/ganache-docker/:/ganache-db
    ports:
      - 8545:8545

  ipfs-daemon:
    image: district0x/ipfs-daemon:latest
    container_name: dev_ipfs-daemon
    volumes:
      - /home/$USER/ipfs-docker:/data/ipfs
    ports:
      - 8080:8080
      - 5001:5001
