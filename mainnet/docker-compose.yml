version: "3"
services:

  mainnet:
    image: parity/parity:stable
    container_name: mainnet_parity
    command:
      --base-path /home/parity/.local/share/io.parity.ethereum/
      --jsonrpc-apis 'web3,eth,net,parity,personal'
      --jsonrpc-interface '0.0.0.0'
      --jsonrpc-hosts none
      --jsonrpc-cors '*'
      --cache-size 6000
      --db-compaction ssd
      --mode active
      --pruning fast
      --logging info
      --geth
      --jsonrpc-port 8545
    ports:
      - 8545:8545
    volumes:
      - /home/$USER/parity-docker/:/home/parity/.local/share/io.parity.ethereum/
    labels:
      - "com.centurylinklabs.watchtower.enable=true"

  ropsten:
    image: parity/parity:stable
    container_name: ropsten_parity
    command:
      --chain ropsten
      --unlock c4e0b92df2de77c077d060e49ec63dc196980716
      --password /home/parity/c4e0b92df2de77c077d060e49ec63dc196980716.pass
      --base-path /home/parity/.local/share/io.parity.ethereum/
      --jsonrpc-apis 'web3,eth,net,parity,personal'
      --jsonrpc-interface '0.0.0.0'
      --jsonrpc-hosts none
      --jsonrpc-cors '*'
      --cache-size 6000
      --db-compaction ssd
      --mode active
      --pruning fast
      --logging info
      --geth
      --jsonrpc-port 8545
    ports:
      - 8546:8545
    volumes:
      - /home/$USER/c4e0b92df2de77c077d060e49ec63dc196980716.pass:/home/parity/c4e0b92df2de77c077d060e49ec63dc196980716.pass
      - /home/$USER/ropsten-docker/:/home/parity/.local/share/io.parity.ethereum/
    labels:
      - "com.centurylinklabs.watchtower.enable=true"

  rinkeby:
    image: ethereum/client-go:stable
    container_name: ropsten_parity
    command:
      --rinkeby
      --rpc
      --rpcapi db,eth,net,web3,personal
      --rpcaddr "0.0.0.0"
      --cache=6000
      --rpcport 8545
      --rpccorsdomain "*"
    ports:
      - 8547:8545
    volumes:
      - /home/$USER/rinkeby-docker/:/root/.ethereum
    labels:
      - "com.centurylinklabs.watchtower.enable=true"

  watchtower:
    image: v2tec/watchtower:latest
    container_name: watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - WATCHTOWER_NOTIFICATIONS=slack
      - WATCHTOWER_NOTIFICATION_SLACK_HOOK_URL=https://hooks.slack.com/services/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
      - WATCHTOWER_NOTIFICATION_SLACK_IDENTIFIER=Mainnet-Watchtower-Service
    command: --label-enable --cleanup --interval 300
